@model Discount

<h2 class="mt-3"><i class="far fa-address-card"></i> Add Discount</h2>

<form method="post" asp-action="Add">
    <div>
    <div class="form-row">
        <div class="form-row">
        <div class="form-group col-md-6">
            <label asp-for="@Model.Code">Code</label>
            <input asp-for="@Model.Code" class="form-control" placeholder="Code" />
        </div>
    </div>
        <div class="form-group col-md-6">
            <label asp-for="@Model.Title">Title</label>
            <input asp-for="@Model.Title" class="form-control" placeholder="Title" />
        </div>
        <div class="form-group col-md-6">
            <label asp-for="@Model.Description">Description</label>
            <input asp-for="@Model.Description" class="form-control" placeholder="Description" />
        </div>
        <div class="form-group col-md-6">
            <label asp-for="@Model.DiscountPercent">DiscountPercent</label>
            <input asp-for="@Model.DiscountPercent" class="form-control" placeholder="DiscountPercent" />
        </div>
        <div class="form-group col-md-6">
            <label asp-for="@Model.ProductId">ProductId</label>
            <input asp-for="@Model.ProductId" class="form-control" placeholder="ProductId" />
        </div>
@*         
        <div class="form-group col-md-6">
            <label>Product</label>
            <select id="ProductId" Name="ProductId">
                @foreach(Product p in ViewBag.Products){
                    @if (p.ProductId == @Model.ProductId){
                        <option selected value="@p.ProductId">@p.ProductName</option>
                    } else {
                        <option value="@p.ProductId">@p.ProductName</option>
                    }
                }
            </select>
        </div> *@
        <div class="form-group col-md-6">
            <label asp-for="@Model.StartTime">Start Time</label>
            <input asp-for="@Model.StartTime" class="form-control" placeholder="StartTime"/>
        </div>
        <div class="form-group col-md-6">
            <label asp-for="@Model.EndTime">End Time</label>
            <input asp-for="@Model.EndTime" class="form-control" placeholder="EndTime" />
        </div>
    </div>
   
    <button type="submit" class="btn btn-outline-primary">Add Discount</button>
</form>